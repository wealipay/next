export default function Home() {
  return (
    <div>
      home龙首页码头作用**：从 Next.js 中导入 `Metadata` 类型（用于定义页面的
      SEO 元数据，如标题、描述等）。 - **注意**：`import type`
      表示仅导入类型（TypeScript 特性），不会增加运行时体积。<span className="font-noto">5.55</span>555.5566
    </div>
  );
}
